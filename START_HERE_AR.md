# تشغيل الموقع ولوحة التحكم

المشروع مهيأ لمستودعك **AliNSheikh/portfolio**.

- رابط الموقع بعد النشر: https://alinsheikh.github.io/portfolio/
- رابط لوحة التحكم بعد النشر: https://alinsheikh.github.io/portfolio/admin/

## رفع الملفات

1. فك ضغط الملف، ثم افتح مجلد `ali-sheikh-portfolio`.
2. احتفظ بنسخة احتياطية من مستودعك الحالي.
3. ارفع **محتويات المجلد** إلى جذر مستودع `portfolio`، بحيث يظهر `package.json` مباشرة في الصفحة الرئيسية للمستودع.
4. تأكد من رفع المجلد المخفي `.github` وبداخله `workflows/deploy.yml`، وكذلك `package-lock.json` وبقية المجلدات. لا ترفع الملف المضغوط وحده، ولا تضع المشروع داخل مجلد إضافي.
5. ارفع التعديلات إلى فرع `main`.
6. من إعدادات المستودع اختر **Settings → Pages → Source → GitHub Actions**.
7. افتح **Actions → Deploy portfolio**، وانتظر اكتمال النشر بنجاح. يمكنك اختيار **Run workflow** إذا لم يبدأ تلقائيًا.

## الدخول إلى لوحة التحكم

تستخدم اللوحة صلاحيات حسابك في GitHub. لا توجد كلمة مرور افتراضية منفصلة.

1. افتح إعدادات حسابك في GitHub، ثم **Developer settings → Personal access tokens → Fine-grained tokens**.
2. أنشئ رمز وصول جديدًا، واختر المالك **AliNSheikh**.
3. اختر **Only select repositories** ثم مستودع **portfolio** فقط.
4. ضمن صلاحيات المستودع، امنح **Contents: Read and write**.
5. أنشئ الرمز وانسخه، ثم افتح رابط لوحة التحكم أعلاه وألصقه في حقل الدخول.

يُحفظ الرمز في ذاكرة جلسة التحرير فقط، ولا يُحفظ داخل ملفات الموقع أو تخزين المتصفح. ستحتاج إلى إدخاله مجددًا بعد تحديث الصفحة أو تسجيل الخروج. لا تضع الرمز داخل ملفات المشروع ولا ترسله في المحادثات.

## إدارة المحتوى

- **Profile & introduction:** الاسم، النبذة، الصورة، موضع قص الصورة، الموقع، السيرة الذاتية، المهارات المختصرة وأزرار المقدمة.
- **Sections & content:** إضافة الأقسام، حذفها، تكرارها، ترتيبها، إظهارها وإخفاؤها، وتعديل عناوينها وعناصرها.
- **Contact & links:** الهاتف والبريد وواتساب وجميع روابط التواصل والأزرار المرتبطة بها.
- **Branding & buttons:** عنوان الموقع، الأيقونة، اسم شريط التنقل، نص التذييل وتسميات الأزرار.
- **Media library:** رفع الصور والشهادات والشعارات وملفات PDF وإدارتها.
- **SEO & Google:** بيانات تحسين محركات البحث، رمز إثبات Search Console ومعرّف Google Analytics.

العناصر الجديدة تبدأ بحالة **Draft**. غيّر الحالة إلى **Published — show after Publish website** عندما يصبح العنصر جاهزًا. الأقسام الفارغة لا تظهر للزوار.

الشهادة المنشورة تحتاج إلى صورة ورابط تحقق. تفتح الصورة وزر عرض الشهادة الرابط نفسه. وفي المقالات، أضف عنوانًا ومحتوى ومعرّف رابط فريدًا، ثم بيانات SEO عند الحاجة.

## الحفظ والنشر

- **Done editing:** يغلق محرر العنصر ويحتفظ بتغييراتك في الجلسة الحالية.
- **Save draft:** يحفظ المسودة في GitHub دون استبدال المحتوى المنشور.
- **Preview:** يعرض معاينة الحاسوب والجهاز اللوحي والهاتف.
- **Publish website:** يتحقق من البيانات ثم ينشرها إلى GitHub. يتحدث الموقع بعد نجاح عملية GitHub Actions.

احفظ المسودة قبل إغلاق المتصفح. لأن مستودعك عام، يمكن قراءة ملفات المسودات من GitHub رغم أنها لا تظهر في الموقع المبني.

## ملاحظات مهمة للتشغيل

لا يكفي فتح `index.html` مباشرة. للتشغيل المحلي تحتاج إلى **Node.js 22.12.0 أو أحدث**؛ إصدار **22.6.0** أقدم من المطلوب. يستخدم النشر التلقائي Node.js 24.

من داخل مجلد المشروع:

```bash
npm ci
npm run dev
```

ثم افتح عنوان Vite المطبوع في الطرفية، مع المسار `/portfolio/` للموقع أو `/portfolio/admin/` للوحة التحكم.

إذا رفضت اللوحة الدخول أو الحفظ، تحقق من صلاحيات الرمز وتاريخ صلاحيته واختيار المستودع الصحيح. وقد تمنع قواعد حماية الفرع النشر المباشر. إذا ظهر الموقع القديم، تحقق من نجاح آخر عملية في Actions ثم حدّث الصفحة.

التعليمات التفصيلية لإعداد Google والنطاق المخصص وحل الأخطاء موجودة في `SETUP.md`.
